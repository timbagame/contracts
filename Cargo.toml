[workspace]
members = [
    "programs/*"
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
strip = true
debug = false
panic = "abort"
overflow-checks = true

[profile.release.build-override]
opt-level = "z"
incremental = false
codegen-units = 1
